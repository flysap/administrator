# here will be set up active theme .
default_theme: flysap/admin-lte

# additional menu module folder, the path is relative to application path ..
module_namespaces:

# here will be setup initial users .
auth:
  admin:
    id: 1
    name: admin
    username: admin
    roles: [admin]
    email: admin@example.com
    role: admin
    password: hashed_passowrd
  editor:
    id: 2
    username: editor
    name: editor
    roles: [editor]
    email:  editor@example.com
    role: editor
    password: hashed_passowrd
